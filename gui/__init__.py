"""Super TTS GUI package — PyQt6 desktop wrapper for the Discord bot.

This package only loads in the Windows installer build (entry point
`gui_main.py`). Docker/Railway deployments use `main.py` directly and
never import anything under `gui/`.
"""
